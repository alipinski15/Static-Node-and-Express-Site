extends layout.pug

block content
    #error
        h1= error.message
        h2= error.status
        p= error.stack
        br
        br
        a(href='/') ← Home
        .img
            img(src="/static/images/404.png")